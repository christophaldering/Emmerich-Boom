export default function AnmeldungPage() {
  return (
    <div style={{
      minHeight: "100svh",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      background: "var(--black)",
      color: "var(--warm)",
      fontFamily: "'Playfair Display', serif",
      fontSize: "1.5rem",
      textAlign: "center",
    }}>
      Anmeldung — wird gebaut
    </div>
  );
}
